% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_barplot.R
\name{create_barplot}
\alias{create_barplot}
\title{Create a Bar Plot with Confidence Intervals and Text Labels}
\usage{
create_barplot(
  data,
  x_var,
  y_var = "mean",
  fill_var = "year",
  group_var = "year",
  lower_ci = "loci",
  upper_ci = "upci",
  label_var = "mean",
  responded_var = "responded",
  cnc_palette,
  x_label = NULL,
  y_label = NULL,
  limits_y = c(0, 4),
  my_theme
)
}
\arguments{
\item{data}{A data frame containing the variables to plot.}

\item{x_var}{Character string specifying the variable for the x-axis.}

\item{y_var}{Character string specifying the variable representing the mean. Defaults to \code{"mean"}.}

\item{fill_var}{Character string specifying the variable used for fill color. Defaults to \code{"year"}.}

\item{group_var}{Character string specifying the variable to group for text color. Defaults to \code{"year"}.}

\item{lower_ci}{Character string specifying the variable for the lower bound of the confidence interval. Defaults to \code{"loci"}.}

\item{upper_ci}{Character string specifying the variable for the upper bound of the confidence interval. Defaults to \code{"upci"}.}

\item{label_var}{Character string specifying the variable for the labels on the bars. Defaults to \code{"mean"}.}

\item{responded_var}{Character string specifying the variable representing sample size or response count. Defaults to \code{"responded"}.}

\item{cnc_palette}{A character vector of colors used for fill and color scales.}

\item{x_label}{Optional character string for the x-axis label.}

\item{y_label}{Optional character string for the y-axis label.}

\item{limits_y}{Numeric vector of length two specifying the y-axis limits. Defaults to \code{c(0, 4)}.}

\item{my_theme}{A ggplot2 theme object (or function returning a theme) to be applied to the plot.}
}
\value{
A ggplot2 plot object.
}
\description{
This function builds a bar plot displaying the mean values along with their confidence intervals.
It further adds text labels for means and sample sizes, allowing programmatic specification of the variable names.
}
\examples{
\dontrun{
  # Example usage with sample data:
  data <- data.frame(
    x = rep(letters[1:5], each = 2),
    mean = runif(10, 1, 3),
    loci = runif(10, 0.5, 1),
    upci = runif(10, 3, 3.5),
    responded = sample(50:100, 10, replace = TRUE),
    year = rep(c("2020", "2021"), 5)
  )

  cnc_palette <- c("#78AAA9", "#FFDB6E", "#604D75", "#A03030",
                   "#3B8EAD", "#8E4921", "#CC79A7", "#B3D7E5")
  my_theme <- ggplot2::theme_minimal()

  p <- create_barplot(data,
                      x_var = "x",
                      y_var = "mean",
                      fill_var = "year",
                      group_var = "year",
                      lower_ci = "loci",
                      upper_ci = "upci",
                      label_var = "mean",
                      responded_var = "responded",
                      cnc_palette = cnc_palette,
                      x_label = "Category",
                      y_label = "Mean Value",
                      limits_y = c(0, 4),
                      my_theme = my_theme)
  print(p)
}

}
